{% macro breadcrumb(leads) %}
  <nav id='breadcrumb'>
    {% for l in leads -%}
      <span>
        <a href="{{ l.link }}">{{ l.text }}</a>
      </span>
      {% if loop.revindex0 !== 0 %}
        {% svg "breadcrumb-seperator" %}
      {% endif %}
    {%- endfor %}
  </nav>
{% endmacro %}